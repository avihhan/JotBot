{
  "name": "jotbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp deploy",
    "open": "clasp open",
    "sync-env": "node scripts/sync-env.js"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.0"
  }
}
