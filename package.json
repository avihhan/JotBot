{
  "name": "jotbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp deploy",
    "open": "clasp open",
    "sync-env": "node scripts/sync-env.js",
    "sync-env-cleanup": "node scripts/sync-env.js --cleanup"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.0"
  }
}
